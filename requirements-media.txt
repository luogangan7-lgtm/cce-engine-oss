# 媒体本体依赖 —— 只在跑 media_ingest 的 job 装, 不进主 requirements。
# 理由: onnxruntime + 模型约 100MB, 让每个 job 都装是白等; 但**跑媒体的 job 必须装**,
# 否则 CI 上 OCR 恒为 not_available, 「全链回放」只跑到降级路径 —— 那是绿而没测到东西。
Pillow>=10.0
rapidocr-onnxruntime>=1.3
c2pa-python>=0.37      # C2PA 真解析: absent(查过确无) 与 not_available(查不了) 才可分
cryptography>=42       # 测试里现场签一张真图, 证明 present 分支可达(不是恒返回 absent)
