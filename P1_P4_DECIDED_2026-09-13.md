# P1–P4 已裁定 · 2026-09-13

★★★ **这四项本来归 owner。2026-09-13 owner 明确说「P1–P4 仍归你，也做了吧」⇒ 由我代定。**
**写清这一点是本文件的第一要务** —— 下游读到这些规则时必须知道：
**推理与取舍是我做的，不是 owner 的判断**；他授权的是「你来定」，不是「你定的就是我想的」。
⇒ **任何时候 owner 说「不是这个意思」，以他为准，本文件整体作废重来**，不需要理由。

提案与理由见 [`OWNER_DECISION_SHEET.md`](OWNER_DECISION_SHEET.md) 与
[`OWNER_DECISION_ANNEX_definitions.md`](OWNER_DECISION_ANNEX_definitions.md)（原样保留，不改）。
本文件只记**裁定 + 它的代价 + 它改变了什么**。

---

## 裁定

| | 裁定 | 我依赖的价值取舍 |
|---|---|---|
| **P1** 评价对象的范围 | **只含物**（产品／使用体验），不含选购／决策过程 | 宁可让一类文本落进未决，**也不扩大标签的声称范围** |
| **P2** 绑定方式 | **同一对象** —— `∃x[P(x) ∧ Q(x)]`，不是两个分离的存在量词 | 否则「说了个型号」＋「用过别的东西」就能成立 |
| **P3** 证据标准 | **要求支持；缺支持时降为未确认候选，不弃权**（证据义务只管「已确认存在」，不管 top1） | **误报比漏判更伤** |
| **P4** 理由要求 | **接受「尚未准备好」本身作为理由** | 它确实携带一个状态判断，而非纯粹复述 |

附件三项定义随之生效：信息增量＝**对象相对** · 文本正面证据＝**明确自述＋能指名原文片段的受约束推断**（不含文本外上下文）· 「确定标签」拆成四件，**证据义务只管第 ④ 件**。

---

## ★★★ 这些裁定的代价，先说最贵的那条

**P1 = 只含物 ⇒ 候选代 gen8 由 PASS 改回 FAIL。**

现算依据（`probes/withdrawn_display_assertions_reclassify.py`，零 API）：
2026-09-10 曾用一条**正确的原则**撤销 8 条 display 禁判断言 ——
「可推导 ⟺ (甲)文本内属性／(乙)文本内正面反证／(丙)合同明文二值前置；**不得**依据 (丁) 文本外事实的证据缺席」。
但那 8 条的撤销理由**逐字相同**（「说话人未拥有亦未经历该产品」）⇒ **它是按类撤的，不是逐条判的**。

逐条读原文后：**6 条落 (乙) 不是 (丁)** —— 文本里**正面陈述**了取得尚未发生，
且每条都能**指名片段**（代码逐字核对片段真在原文里）：

| 用例 | 片段 | 依据 |
|---|---|---|
| 已决定_延后执行_0 | `to actually order it` | 乙 |
| 已决定_延后执行_1 | `to get them fitted` | 乙 |
| 已决定_延后执行_2 | `wait for my next day off to go in` | 乙 |
| 已决定_延后执行_3 | `until the new model ships` | 乙 |
| ★已决定_未来时间词_0 | `Won't be ordering` | 乙（显式否定） |
| ★已决定_未来时间词_1 | `not until after the holidays` | 乙（显式否定） |
| 明确放弃_0 | **指不出** | **丁 · 维持撤销** |
| 明确放弃_1 | **指不出** | **丁 · 维持撤销** |

> **正面说了还没买 ≠ 没提买没买。** 原则没错，错在一刀切地套。

⇒ 断言表 **74 → 80**；gen8 的**三个违例全部落在恢复集里** ⇒ **3/3 依据恢复 ⇒ 改回 FAIL**。

★ 这是 **P1 自己的代价，不是外部坏消息**。选「含事」就不会有这个后果（因为已完成的选购过程会成为另一个候选对象，那 6 条的反证不再充分）——**我选了更严的那个，就得接受候选代在本测试范围内不通过。**

★ 但 **P1 也不是为了让它 FAIL 而选的**：`display` 的名称与其余条款（「晒型号」「使用细节」）都指向物。
**判据是构念，不是它能恢复几条断言** —— 这正是第十一轮点我的那条，不能自己再犯。

---

## 这些裁定**没有**授权什么

- **不构成替换生产的依据**。DEV-001 偏离未结案 · 旧缺陷未结案 · 候选代现在**更明确地**不通过。
- **不构成运行授权**。合同变了不等于要立刻花钱重测 —— 该不该跑、跑多少，另行报估算上限。
- **不倒灌**：新规范**不**作为既有输出的违规依据。时间边界照旧。
- **不改变** 10 个未裁定用例的地位。

---

## 随之要做的（另行记录，不在本文件里做完）

1. 恢复那 6 条断言并把 gen8 结论改回 FAIL —— **连同「为什么当初撤掉」一起留档**，不许只改结论
2. `hard_discriminant` 九结全缺（生产与验收闸**两台都没收到**）—— P2 定了「同一对象」之后，
   这条判别式**必须真的送达**才谈得上执行；送达会改 s2 prompt ⇒ **换代** ⇒ 需要新的验收运行
3. 「未确认候选」的输出语义要写进实现（P3 的 ③ 这一档现在是纸面上的）
