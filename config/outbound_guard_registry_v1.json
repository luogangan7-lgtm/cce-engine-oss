{
  "kind": "cce.outbound_guard_registry.v1",
  "version": "1.0.0",
  "profiles": {
    "hearing_aid": {
      "allowed_domains": ["hearing_aid"],
      "market": "intl",
      "implementation": "scripts/cce_outbound_guard.py"
    }
  },
  "rule": "A missing domain guard blocks outbound publication but never changes the platform adapter or the CCE measurement ontology."
}
