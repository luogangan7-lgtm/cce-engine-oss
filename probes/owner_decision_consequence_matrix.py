#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""owner 两个待决格的**后果分析矩阵** —— 零 API, 用已有三代读数现算。

★★★ 这是什么 / 不是什么(web GPT 第九轮原话):
  「准备规范决策材料, **不替 owner 选择**。…然后展示: **不同候选定义会改变哪些案例的可判性和结论**
    —— 这些是**待批准的后果分析, 不是现行断言**。」
  ⇒ 本文件**不产生任何现行断言**, 不改断言表, 不改判任何一轮。

★★★ 为什么是矩阵而不是单选(同一轮 GPT):
  「owner 仅给出一个 display 对象域名称**可能仍然不够**。至少还需明确: 条件究竟作用于哪个对象、
    『已拥有或已经历』怎样关联到它, 以及**缺乏文本证据时验收应当如何处理**。
    否则只是把空白从『对象域』移到『证据规则』。」
"""
import itertools, json, pathlib

ROOT = pathlib.Path(__file__).resolve().parent.parent
# ★ 本矩阵**不需要**题库原文 —— 它只算「哪些用例的可判性会变」, 用的是 id 集合与三代读数。
#   第一版顺手读了保险库题库却**赋值后从未使用**, 被 test_cce_no_offrepo_dependency 判红。
#   修法沿用同一个闸上次教我的: **删掉未用的依赖**, 而不是给它加一个降级分支。

DECIDED_NOT_OBTAINED = ["C_已决定_延后执行_0", "C_已决定_延后执行_1", "C_已决定_延后执行_2",
                        "C_已决定_延后执行_3", "C_★已决定_未来时间词_0", "C_★已决定_未来时间词_1"]
ABANDONED = ["C_明确放弃_0", "C_明确放弃_1"]
BOOKMARK  = ["C_仅收藏_0", "C_仅收藏_1"]


def readings():
    old = {r["id"]: r["top1"] for r in json.loads(
        (ROOT / "tests/data/production_suspend_defect_result.json").read_text(encoding="utf-8"))["★逐题"]}
    g7 = {r["id"]: r.get("top1") for r in json.loads(
        (ROOT / "results/gen7_candidate/rows.json").read_text(encoding="utf-8"))}
    g8 = {r["id"]: r.get("top1") for r in json.loads(
        (ROOT / "results/gen8_shipping/rows.json").read_text(encoding="utf-8"))}
    return {"旧代": old, "候选·树关": g7, "上线·树开": g8}


# ── 第三格: display 对象域 × 证据规则 × 回退 ──────────────────────────
DOMAIN = {"甲·限物": "谈论对象只能是**物**(产品/使用体验)",
          "乙·物+事": "也允许**事**(已完成的选购决策过程)"}
# ★★★★ 2026-09-11 web GPT 第十轮: **我把两种不同的「禁判」混成了一个轴 —— 这是根错。**
#   E1 与 E2 **并不互斥, 完全可以同时执行**:
#     · E1「必须有文本正面证据」= **输出资格规则**(证据不足 ⇒ 不准作出这个确定标签)
#     · E2「沉默不构成否定」    = **事实推理规则**(文本没说 ≠ 现实中没发生)
#   ⇒ 可以同时成立: 「是否已经历」**尚未知** ∧ 「缺少合同要求的支持 ⇒ **不具备确定输出资格**」
#     —— **这里没有把「未知」变成「假」**(W3C 开放世界假设: 事实语义与合规检查不是同一层)。
#   ⇒ 正确的轴**不是** E1/E2 二选一, 而是**两种禁判分别成不成立**。
BAN_KIND = {
 "①反证型": {"想判断": "文本**能够反证**某项既有必要条件 ⇒ 原标签**违反**该条件",
          "所需依据": "对该必要条件**或整个合取组合**的**有效反证**",
          "★注意": "「无法证明某一项不成立」**不总能推出**「无法证明它们不可能同时成立」 —— "
                "有时不知道哪一项不成立, **却能证明按对象绑定规则找不到能同时承担两者的合法对象**"},
 "②资格型": {"想判断": "标签**缺少合同要求的支持** ⇒ **不准确定输出**",
          "所需依据": "合同**已经规定**的证据义务 + 该义务未满足的事实",
          "★关系": "**不需要先证明①**; 反过来, **也不能被留档成①**",
          "★★★时间边界": "旧合同若**没有**规定「文本必须正面证明」, 现在提出证据义务"
                   "**不能倒灌成旧输出的新违规依据** —— 只能作为**待批准的新规范**"},
}
EVIDENCE = {"E1·有证据义务": "新规范**要求**对**共同对象**提供正面支持(启用②资格型禁判)",
            "E2·无证据义务": "不设证据义务(现状) ⇒ **只剩①反证型**这一条路"}
FALLBACK = {"F1·弃权": "判别式全不满足时**弃权**(合同已有 empty_knots_is_abstain_v1)",
            "F2·强制择一": "维持现状: 必须从九结里挑一个"}


def cell3(domain, evidence, fallback):
    """★★★★ 2026-09-11 按第十轮**重写**。原版结论「只有甲+E1 禁得动」**已撤回**, 两个原因:
      ① 在**乙**的对象域下, 若新规范要求对**共同对象**提供正面支持而某条文本提供不了,
         **仍可禁止确定输出** —— 此时禁的是**无支持的输出**, 不是证明现实中的经历不存在。
      ② **乙允许更多对象 ≠ 这些对象可以任意替换**。只要对象绑定与条件对应关系有明确约束,
         **乙下也可能出现有效反证**。
    ⇒ 现在只报**两种禁判各自的可用性**, **不下「只有甲禁得动」这种一般结论**。
    """
    # ② 资格型: 只要设了证据义务就可用, **与对象域无关**
    qualification_ban = evidence == "E1·有证据义务"
    # ① 反证型: 需要**有效反证**。在甲下「未取得」正面否定第一支, 但第二支**仍需独立否定**
    #    ⇒ 我**没有**证明它在甲下成立, 更没证明它在乙下不成立。**两边都登记为未定。**
    refutation_ban = "**未定** —— 需要对象绑定规则与信息增量定义才判得了"
    bans = qualification_ban
    affected = DECIDED_NOT_OBTAINED + ABANDONED if bans else []
    return {"②资格型禁判可用": qualification_ban,
            "①反证型禁判": refutation_ban,
            "禁 display": bans, "受影响用例": affected,
            "断言数变化": f"74 → {74 + len(affected)}" if bans else "74(不变)",
            "★可判性": (f"{len(affected)} 个用例**恢复可裁定**" if bans
                     else "那 8 个用例**仍不可裁定**(归入未决)"),
            "★为什么": ("**设了证据义务** ⇒ ②资格型禁判可用 —— 禁的是「**无支持的输出**」, "
                     "**不是**证明现实中的经历不存在。★ 这条**与对象域无关**(甲乙都适用)"
                     if bans else
                     ("**没设证据义务** ⇒ 只剩①反证型, 而①在本分析下**未定**。"
                      "★ **理由已收紧(2026-09-11 自查)**: 我原写「选购过程**确实已经历** ⇒ display **成立**」—— "
                      "那**比需要的强**。display 是**合取**(信息增量 ∧ 对象已拥有/已经历), "
                      "若对象取「选购过程」, 则信息增量也得是**关于那个过程**的, "
                      "而这六条给的增量是**型号**(产品事实) ⇒ 「成立」推不出来。"
                      "★ 但**格子的判定不变**: 要禁判需证明**某一必要条件不成立**, "
                      "而乙下**两支都证不伪** ⇒ **禁不动**。"
                      "⇒ 可辩护的理由是「**无法证明任一必要条件不成立**」, 不是「display 成立」。"
                      if domain == "乙·物+事" else
                      "对象是物, 但 E2 下**沉默不构成否定** ⇒ 第二支否不掉 ⇒ 禁不动 —— "
                      "**这正是 2026-09-10 撤销那 8 条的理由, 只给「甲」不改证据规则等于什么都没定**")),
            "★F 的作用": ("弃权会把不可裁定项**显式记为弃权**而非塞给某个结"
                       if fallback == "F1·弃权" else
                       "强制择一 ⇒ 模型仍会挑一个, 不可裁定项**在输出上看不见**")}


# ── 第一格: 理由要求 × 允许证据 × 不足时 ───────────────────────────────
REASON = {"A·需非同义反复理由": "仅重述推迟本身**不算**理由",
          "B1·不要求理由": "有推迟表述即满足",
          "B2·「尚未准备好」算一类": "仍要求理由, 但把它列为可接受的一类"}
CONTEXT = {"仅当前句": "只看被判定的这一句", "D·固定上下文": "允许从**事先规定**的上下文取证"}


def cell1(reason, context, fallback):
    b0_banned = reason == "A·需非同义反复理由"          # "Saving this for when I'm ready."
    # ★★★ B1 的连带风险(GPT 点名): 「有推迟表述即满足」可能连「决策未定」也一起改掉
    b1_spill = reason == "B1·不要求理由"
    spilled = DECIDED_NOT_OBTAINED if b1_spill else []
    return {"C_仅收藏_0 是否禁 suspend": b0_banned,
            "★★★B1 的连带": (f"**「已决定但尚未取得」{len(spilled)} 条的 suspend 禁判可能一并失效** —— "
                        "它们现在靠「决策被明确悬置」为假而禁; 若「有推迟表述即满足」把这一条也放宽, "
                        "**74 条里的 suspend 那 12 条要重审**。★ GPT: 若真想把所有延后行动纳入, "
                        "**那是另一个构念范围变化, 需单独明说**。" if b1_spill else "无"),
            "受影响用例": (BOOKMARK[:1] if b0_banned else []) + spilled,
            "★★D 在本语料上无区别": ("**是** —— 这 22 条是孤立构造题, **没有上下文** ⇒ "
                          "D 与「仅当前句」在本语料上**产出完全相同**。"
                          "⇒ 选 D 需要**另建带上下文的语料**才检验得了, 本轮数据**测不出它的效果**。"
                          if context == "D·固定上下文" else "不适用"),
            "★F 的作用": ("弃权 ⇒ C_仅收藏_0 记为弃权" if fallback == "F1·弃权"
                       else "强制择一 ⇒ 仍会判某个结(三代实测都是 suspend)")}


def build():
    R = readings()
    m3 = {}
    for d, e, f in itertools.product(DOMAIN, EVIDENCE, FALLBACK):
        m3[f"{d} × {e} × {f}"] = cell3(d, e, f)
    m1 = {}
    for r, c, f in itertools.product(REASON, CONTEXT, FALLBACK):
        m1[f"{r} × {c} × {f}"] = cell1(r, c, f)

    bans3 = [k for k, v in m3.items() if v["禁 display"]]
    return {
      "block": "OWNER_DECISION_CONSEQUENCE_MATRIX",
      "★zero_api": "用已有三代读数现算, **零调用**。",
      "★★★这是什么": "**待批准的后果分析, 不是现行断言。** 不改断言表, 不改判任何一轮, 不替 owner 选。",
      "★★★★性质必须固定为(第十轮)": "**旧输出在不同假定规范下的后果重算。** "
                     "它**不是**「新规范送给模型后, 新输出将是什么」的证据。"
                     "**不要把后果分析再升成换代效果预测。**",
      "★★该补的不是更多自由轴, 是先把层次固定(第十轮)": {
        "对象绑定规则": "由**规范**规定怎样从文本及允许上下文中确定对象; 要能说明两个必要条件**是否指向同一对象**。"
                "**不能在某个条件失败后临时换一个对象救判。** 多个对象是否允许、是否任一满足即可, "
                "**必须由规范决定, 不能由当次判官临场选择。**",
        "★不宜": "再简单增加一个二值轴然后把组合翻倍"},
      "★两种禁判(第十轮)": BAN_KIND,
      "三代读数": {k: R[k] for k in R},
      "★三代都没变过的用例": sorted(i for i in R["旧代"]
                            if R["旧代"][i] == R["候选·树关"].get(i) == R["上线·树开"].get(i)),

      "第三格·display 对象域": {
        "选项轴": {"对象域": DOMAIN, "证据规则": EVIDENCE, "回退": FALLBACK},
        "组合数": len(m3),
        "★★★能启用②资格型禁判的组合": bans3,
        "★★★★最要紧的一条(2026-09-11 **已改写**)": (
          f"**{len(bans3)}/{len(m3)}** 个组合能启用**②资格型禁判**, 而它们的共同点**不是对象域, 是「设了证据义务」** —— "
          "**甲乙都能启用**。⇒ 决定禁判能力的是**证据义务这一轴**, 不是对象域。"),
        "★★★★原结论已撤回": (
          "原写「**只有 2/8 个组合(且都是甲)禁得动**」⇒ **撤回**。两个原因(web GPT 第十轮): "
          "① 在**乙**下若要求对**共同对象**提供正面支持而文本提供不了, **仍可禁止确定输出**"
          "(禁的是**无支持的输出**, 不是证明现实中经历不存在); "
          "② **乙允许更多对象 ≠ 对象可任意替换** —— 对象绑定有约束时**乙下也可能出现有效反证**。"),
        "★★我原来那句也要改准": (
          "原写「只回答对象域, 空白便移到证据规则」—— **提醒 owner 不能把局部决定当完整合同这点正确**, "
          "但更准确的说法是: **「对象域这一处被定了, 证据义务与欠定处理仍未定」**。"
          "**不是**对象域决定没有价值, 也不一定只是空白搬家。"
          "★★ **本样本计数相同, 不代表规则等价。**"),
        "逐组合": m3,
      },

      "第一格·收藏+未来准备状态": {
        "选项轴": {"理由要求": REASON, "允许证据": CONTEXT, "不足时": FALLBACK},
        "组合数": len(m1),
        "★★★A 是唯一能禁 C_仅收藏_0 的理由档": [k for k, v in m1.items() if v["C_仅收藏_0 是否禁 suspend"]][:4],
        "★★★B1 有连带风险 —— **2026-09-11 已核实撤回**": "★ 旧说法(**已撤回**): 「选 B1 会让『已决定但尚未取得』6 条的 suspend 禁判可能一并失效, 12 条 suspend 断言要重审」。**两重错**: ① 逻辑上原要求是 `决策悬置 ∧ 理由`, 取消「理由」剩下的是「决策悬置」, **不是连它一起取消**; ② 事实上 probes/p4_reason_coupling_audit.py 从冻结断言文件现算 —— 那 12 条的失败合取项**无一例外**是「决策被明确悬置」, 与「理由」**无关** ⇒ **受影响 0 条**。**这项代价不存在。**",
        "★★★★D 在本语料上测不出": "这 22 条是**孤立构造题, 没有上下文** ⇒ "
                      "「D·固定上下文」与「仅当前句」在本语料上**产出完全相同**。"
                      "⇒ **选 D 需要另建带上下文的语料**, 本轮数据**无法检验它**。",
        "逐组合": m1,
      },

      "★★两格之间的耦合": "第三格的 **F(回退)** 与第一格的 **F(不足时)** 是**同一条规则** —— "
              "「判别式不满足时怎么办」。**可以一次决定, 但一旦决定就同时作用于两格**。",

      "★★★本矩阵不产生的东西": [
        "**不产生**任何现行断言 —— 断言表仍是 74 条",
        "**不产生**对任何一轮的改判",
        "**不产生**「哪个选项更好」的推荐 —— 那是规范决定",
        "**不产生**「三代读数支持某个选项」—— 读数是**当前实现的行为**, 不是规范正确性的证据",
      ],
    }


if __name__ == "__main__":
    print(json.dumps(build(), ensure_ascii=False, indent=1))
